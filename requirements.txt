# NPU 910B 环境依赖包
# 注意：vLLM需要使用华为Ascend版本

# 核心依赖
torch>=2.1.0+ascend  # NPU版本的PyTorch
torch-npu>=2.1.0     # PyTorch NPU扩展
vllm-ascend          # 华为Ascend版本的vLLM (需要从源码编译安装)
transformers-stream-generator

# HEBO相关依赖
numpy>=1.16,<1.25
pandas>=1.0.1
pymoo>=0.6.0
scikit-learn>=0.22
gpytorch>=1.4.0
GPy>=1.9.9
catboost>=0.24.4
xgboost
lightgbm
disjoint-set
matplotlib

# 容器环境额外依赖
psutil>=5.8.0        # 系统监控
setuptools>=65.0.0   # 包管理
wheel>=0.38.0        # wheel包构建

# 注意事项：
# 1. torch和torch-npu需要从华为官方源安装
# 2. vllm-ascend需要从GitHub源码编译安装
# 3. 建议在容器中使用预构建的基础镜像
